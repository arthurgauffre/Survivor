// import { render } from "@testing-library/react";
// import RootLayout from "@/app/layout";
// import "@testing-library/jest-dom"; // Provides custom matchers for assertions
import { describe, it, expect, test } from "vitest";

// describe("RootLayout", () => {
//   it("renders correctly", () => {
//     const { container } = render(
//       <RootLayout>
//         <div>Test Child</div>
//       </RootLayout>
//     );

//     // Check that the HTML structure is correct
//     const htmlElement = container.querySelector("html");
//     expect(htmlElement).toBeInTheDocument();
//     expect(htmlElement?.getAttribute("lang")).toBe("en");

//     // Check that the body has the correct classes
//     const bodyElement = container.querySelector("body");
//     expect(bodyElement).toBeInTheDocument();
//     expect(bodyElement?.className).toContain("min-h-screen");
//     expect(bodyElement?.className).toContain("bg-[#f5f6fa]");
//     expect(bodyElement?.className).toContain("text-[#334760]");

//     // Check that the children are rendered correctly
//     expect(container.textContent).toContain("Test Child");
//   });
// });

describe("Accordion test aze", () => {
  test("Should show title", () => {

  })
})